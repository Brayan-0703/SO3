Comandos usados practica 1

SERVER

sudo apt install nfs-kernel-server

sudo mkdir -p /srv/nfs/SO3
sudo touch /srv/nfs/SO3/adrian{1..100}.txt
ls /srv/nfs/SO3
sudo chown nobody:nogroup /srv/nfs/SO3
sudo chmod 777 -R /srv/nfs/SO3

sudo nano /etc/exports
/srv/nfs/SO3 10.0.0.110(rw,sync,no_subtree_check)

sudo exportfs -a
sudo systemctl restart nfs-kernel-server

cat /srv/nfs/SO3/adrian1.txt
echo adios > /srv/nfs/SO3/adrian1.txt
cat /srv/nfs/SO3/adrian1.txt

CLIENTE

sudo apt install nfs-common

sudo mkdir -p /mnt/SO3
sudo mount 10.0.0.109:/srv/nfs/SO3 /mnt/SO3
ls /mnt/SO3

echo hola > /mnt/SO3/adrian1.txt
cat /mnt/SO3/adrian1.txt 
cat /mnt/SO3/adrian1.txt

sudo nano /etc/fstab
10.0.0.109:/srv/nfs/SO3 /mnt/SO3 nfs defaults 0 0

sudo mount -a
sudo systemctl daemon-reload
reboot

ls /mnt/SO3

============================================

Comandos usados practica 2

sudo apt install samba -y

sudo mkdir -p /srv/samba/SO3
sudo touch /srv/samba/SO3/adrian{1..100}.txt
sudo chmod 777 -R /srv/samba/SO3
sudo nano /etc/samba/smb.conf
[SO3]
path = /srv/samba/SO3
browsable = yes
writable = yes
guest ok = no
read only = no

sudo smbpasswd -a brayan

sudo systemctl restart smdb

cat /srv/samba/SO3/adrian99.txt

============================================

Comandos usados practica 3

ip a
sudo nano /etc/hosts
ip  kali.so3.inet kali

sudo apt install -y samba krb5-user winbind smbclient
Default Kerberos version 5 realm:
so3.inet
Ok
Ok

sudo systemctl stop smbd nmbd winbind systemd-resolved
sudo systemctl disable smbd nmbd winbind systemd-resolved
sudo mv /etc/samba/smb.conf /etc/samba/smb.conf.bak
sudo samba-tool domain provision --user-rfc2307 --interactive
1 enter
2 enter
3 enter
4 enter
8.8.8.8
Brayan20250703@
Brayan20250703@

sudo rm /etc/resolv.conf
sudo nano /etc/resolv.conf
nameserver 127.0.0.1
domain so3.inet

sudo systemctl unmask samba-ad-dc
sudo systemctl start samba-ad-dc
sudo systemctl enable samba-ad-dc

sudo adduser lanegracubana
Brayan20250703@
Brayan20250703@
full enter

sudo samba-tool user create lanegracubana Brayan20250703@

WINDOWS
Panel de control > Redes e Internet > Centro de redes y recursos compartidos > Cambiar configuracion del adaptador > Propiedades de Ethernet > Propiedades de IPv4 > DNS cambiar a ip del servidor, aceptar, cerrar.

Explorador del archivo > Este equipo > Propiedades > Configuracion avanzada del sistema > Nombre del equipo > Cambiar > Dominio, copiar nombre de equipo, Dominio: so3.inet

Administrator/Administrador
Brayan20250703@
aceptar
reiniciar

Otro usuario
lanegracubana
Brayan20250703@

============================================
